# Media Backup Server 0.3.16

- “新建实例”直接使用默认名称创建，登录后进入实例列表，不再弹出名称输入框。
- 实例列表加入删除入口；配对详情统一显示账户名、不可变内部账户 ID 和可更换密码。
- 实例统计采用 Host 风格的“总数 / 在线”表达，移除待配对、启用/全部实例统计。
- 新建及更换密码统一为 32 位小写英文字母或数字；移动 Client 仍只输入授权码。管理 overview 按账户名一次返回全部实例并稳定排序。
- 升级至 Foundation 0.8.8，并同步更新当前 Web 资产与发行合同指纹。
