# Media Backup Server 0.3.7

The Server and administrator Web now consume Foundation 0.7.11 directly from the locked Rust and npm packages. The duplicated Platform Router and Web Shell source snapshots have been removed.

Build metadata derives and tests the exact Foundation revision from `Cargo.lock`. Release packaging, deployment verification, and WSL installation now share the updated immutable Web and release-contract identities.
