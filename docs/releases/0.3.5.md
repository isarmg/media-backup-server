# Media Backup Server 0.3.5

管理 Web 升级到 Foundation Server 0.7.6。总览菜单到首个内容区块及相邻业务区块统一使用 16px 内容间距，不再由浏览器标题外边距和产品局部 margin 分别控制。

发行构建器、安装器和运行时校验已重新绑定到当前 Web 资源摘要与 release contract，避免内容更新后被旧身份常量拒绝。

Chromium 与 Firefox 的总览、备份账号管理、账户设置、移动布局和 WCAG AA 回归通过。
