# Media Backup Server 0.3.9

- 备份实例创建改为与 Host/Sunshine 一致的单步流程：只填写实例名称，服务端原子创建自动存储、100 GiB 默认配额、客户端实例和长期授权码。
- 删除“先创建备份用户、再创建客户端实例”的管理入口；空实例终态删除时同步清理内部存储归属。
- 升级到不可变 Foundation 0.8.1 Rust/npm 依赖与完整 source revision，并同步无实例侧栏的外观快照。
